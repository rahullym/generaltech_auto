services:
  postgres:
    image: postgres:17-alpine
    container_name: gta-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: payload
      POSTGRES_PASSWORD: payload
      POSTGRES_DB: gta
    ports:
      - '5434:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U payload -d gta']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
